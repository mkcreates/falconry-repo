// define meta data
export const metadata = {
    title: process.env.APP_NAME + ' : Add Feeds',
}

export default function RootLayout({ children }) {
  return (
    <>
        {children}
    </>
  )
}
